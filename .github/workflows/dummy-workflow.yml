# TODO:
# When this is called from another branch, make sure you can echo the parameter provided by the other branch. This should be branch name. 
name: dummy

on:
    workflow_call:
        inputs:
            branch-name:
                description: 'The branch to do nothing on'
                required: true
                type: string
    push:
        branches:
        - integ-tests

jobs:
    do-nothing:
        name: Do Nothing
        runs-on: ubuntu-latest

        steps:
            - name: First Step of Nothing
              if: github.event_name == 'workflow_call'
              run: |
                echo "I'm being invoked by the following branch: ${{ inputs.branch-name }}"
            - name:
              if: github.event_name == 'push'
              run: |
                echo "I'm being invoked by push"


